const Admin = require('./Admin');
const QrSchema = require('./QrCode');
const LogosSchema = require('./logo');
const TemplateSchema = require('./Template');

module.exports = {
    Admin,
    QrSchema,
    LogosSchema,
    TemplateSchema
};
